# Expo public env — EXPO_PUBLIC_* values are baked into the client bundle at
# build time (they are public by design). Canonical build-time values live in
# eas.json build profiles / EAS secrets.
EXPO_PUBLIC_BACKEND_URL=
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
